# Socle
SQLAlchemy[asyncio]>=2.0,<3.0
aiosqlite>=0.20
httpx>=0.27
pydantic>=2.7
PyYAML>=6.0
pandas>=2.2

# Tests
pytest>=8.0
pytest-asyncio>=0.23

# --- À décommenter au fil des couches suivantes ---
# asyncpg>=0.29            # PostgreSQL / TimescaleDB en production
# alembic>=1.13            # migrations versionnées
# pvlib>=0.11              # géométrie solaire, ciel clair, modèles PV
# scikit-learn>=1.5        # pipeline de machine learning
# fastapi>=0.111           # exposition des séries et prévisions
# uvicorn[standard]>=0.30
